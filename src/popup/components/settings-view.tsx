/**
 * 设置视图（T2 占位）。
 * 完整表单（API 地址 / API Key / 默认模型 / 保存与校验）由 T4 实现，
 * 布局依据 docs/layouts/示意图-设置页面.md。
 */
export default function SettingsView() {
  return (
    <section className="settings-view">
      <p className="settings-view__placeholder">设置项将在后续版本提供。</p>
    </section>
  );
}
