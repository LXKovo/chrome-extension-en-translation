/**
 * T2 阶段用于验证结果区渲染能力的静态示例 Markdown。
 * 待 T6 接入真实翻译流后移除。
 */
export const SAMPLE_MARKDOWN = `# 大语言模型如何改变软件开发

> 作者：Jane Doe
> 原文链接：https://example.com/how-llms-change-software

过去十年里，软件开发的效率提升主要来自**工具链**的进步。而大语言模型正在把这种提升推向新的阶段。

## 主要变化

1. 代码补全从「按行」变为「按意图」
2. 文档与代码之间的鸿沟被显著缩小
3. 测试用例可以由模型初步生成

### 一个简单例子

\`\`\`typescript
const greet = (name: string): string => 'Hello, ' + name + '!';
\`\`\`

| 场景 | 传统方式 | 引入模型后 |
| --- | --- | --- |
| 代码审查 | 人工逐行 | 模型预筛 + 人工确认 |
| 重构 | 手动替换 | 语义级批量改写 |

> 需要强调的是，模型输出仍然需要人来负责。
`;
